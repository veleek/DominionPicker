<phone:PhoneApplicationPage 
    x:Class="Ben.Dominion.ResultsViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="696" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True">

    <Grid x:Name="LayoutRoot" Background="Transparent" DataContext="{Binding Current, Source={StaticResource PickerState}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">
            <TextBlock x:Name="ApplicationTitle" Text="Dominion Picker" Style="{StaticResource PhoneTextNormalStyle}"/>
            <TextBlock x:Name="PageTitle" Text="Card List" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/>
            <Border Visibility="Collapsed" Margin="3" Background="Green">
                <Grid Margin="24,0,0,0">
                    <TextBlock Margin="2,3,0,0" Text="SampleCard" FontFamily="{StaticResource TrajanPro}" FontSize="42" Foreground="Black"/>
                    <TextBlock Margin="0" Text="SampleCard" FontFamily="{StaticResource TrajanPro}" FontSize="42" Foreground="White"/>
                </Grid>
            </Border>
        </StackPanel>

        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">
            <ScrollViewer>
                <ItemsControl x:Name="CardsList" ItemsSource="{Binding CardList}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="3" Background="{Binding BackgroundColor}">
                                <toolkit:GestureService.GestureListener>
                                    <toolkit:GestureListener Flick="CardItem_Flick" />
                                </toolkit:GestureService.GestureListener>
                                <Grid Margin="24,0,0,0">
                                    <TextBlock Margin="2,3,0,0" Text="{Binding Name}" Style="{StaticResource PhoneTextExtraLargeStyle}" Foreground="Black"/>
                                    <TextBlock Margin="0" Text="{Binding Name}" Style="{StaticResource PhoneTextExtraLargeStyle}" Foreground="White"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
 
    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="True">
            <shell:ApplicationBarIconButton IconUri="/Images/appbar.refresh.png" Text="Refresh" Click="Refresh_Click"/>
            <shell:ApplicationBarIconButton IconUri="/Images/appbar.alpha.png" Text="Change Sort" Click="Sort_Click"/>
            <shell:ApplicationBar.MenuItems>
            </shell:ApplicationBar.MenuItems>
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

</phone:PhoneApplicationPage>
